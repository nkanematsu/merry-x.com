        <% provide(:title, "#{@article.title} | お知らせ | メリエックス株式会社") %>
        <% provide(:description, "メリエックス株式会社からのお知らせ | #{@article.title}") %>
        <% provide(:keywords, 'オフィス清掃,ビル清掃,マンション清掃,クリニック清掃,介護福祉施設清掃,ハウスクリーニング,news,お知らせ') %>
        <% provide(:h1, 'メリエックス株式会社からのお知らせ') %>

        <main class="container news">
            <nav class="breadcrumbs">
                <ol>
                    <li><%= link_to 'トップ', root_path %></li>
                    <li><%= link_to 'お知らせ', news_path %></li>
                    <li><%= link_to @article.title, article_path(@article) %></li>
                </ol>
            </nav>
            <article>
                <h1><%= @article.title %></h1>
                <time datetime="<%= @article.created_at %>">投稿日: <%= l @article.created_at %></time>
                <pre><%= @article.body %></pre>
            </article>
        </main>
